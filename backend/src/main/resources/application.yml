server:
  port: 13001

management:
  endpoints:
    web:
      exposure:
        include: health

spring:
  datasource:
    url: jdbc:sqlite:${LEADER_DATA_DIR:/home/claude/leader-data-dev}/leather-patterns.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: update
    open-in-view: false
